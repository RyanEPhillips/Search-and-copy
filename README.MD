# 🎶 MP3 File Copier

This Python script copies all `.mp3` files from a **source folder** to a **destination folder**, skipping duplicates if they already exist in the destination.

---

## 🧰 Requirements
- Python 3.7 or higher
- Built-in modules: `os`, `shutil` (no extra installation needed)

---

## ⚙️ Usage

1. Open the script and set your folder paths:
   ```python
   source_folder = "C:\\Path\\To\\Source"
   destination_folder = "C:\\Path\\To\\Destination"

   bash
   '''
   python Search_Copy.py